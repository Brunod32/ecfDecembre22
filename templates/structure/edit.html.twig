{% extends 'base.html.twig' %}

{% block title %}Modification de la structure{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-2">Modifier la structure</h1>
            </div>
        </div>
    </div>

    <div class="m-5">

        {{ include('structure/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_structure_index') }}" role="button" class="btn btn-outline-success mt-2">Retour à la liste</a>

        {{ include('structure/_delete_form.html.twig') }}
    </div>
{% endblock %}
