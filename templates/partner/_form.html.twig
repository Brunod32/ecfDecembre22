{{ form_start(form) }}
    {{ form_row(form.name, {
        label: 'Nom du partenaire'
    }) }}
    {{ form_row(form.email, {
        label: 'Email'
    }) }}
    {{ form_row(form.password, {
        label: 'Mot de passe'
    }) }}
    {{ form_row(form.address, {
        label: 'Adresse'
    }) }}
    {{ form_row(form.description, {
        label: 'Description'
    }) }}
    {{ form_row(form.active, {
        label: 'Actif'
    }) }}
    {{ form_row(form.sellFood, {
        label: 'Vente de nourriture'
    }) }}
    {{ form_row(form.sellDrink, {
        label: 'Vente de boisson'
    }) }}
    {{ form_row(form.sendNewsletter, {
        label: 'Envoi de newsletter'
    }) }}
    {{ form_row(form.scheduleManagement, {
        label: 'Gestion de planning'
    }) }}
    {{ form_row(form.privateLesson, {
        label: 'Cours particuliers'
    }) }}
    {{ form_row(form.technicalTeamId, {
        label: 'Identifiant de l\'équipe technique'
    }) }}
    <button class="btn btn-outline-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
